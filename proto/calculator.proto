syntax = "proto3";

package calculator;

service Calculator {
    rpc Add (CalculationRequest) returns (CalculaionResponse);
}

message CalculationRequest {
    int64 a = 1;
    int64 b = 2; 
}

message CalculaionResponse {
    int64 result = 1;
}